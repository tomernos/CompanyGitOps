# Monolithic Chart Backup

This folder contains the **original monolithic Helm chart** before migration to microservices architecture.

## Contents
- `templates/` - Original monolithic templates (all services in one)
- `values.yaml` - Original default values
- `values-prod.yaml` - Original production values

## Purpose
**Reference only** - kept for comparison and migration guidance.

The new architecture uses **subcharts** in `../charts/` directory.

## Date Backed Up
November 22, 2025
