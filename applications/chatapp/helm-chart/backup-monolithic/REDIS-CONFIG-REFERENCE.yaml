# BACKUP - Original monolithic values.yaml

# Redis configuration (lines 157-178 from original):
redis:
  enabled: true
  replicas: 1
  
  image:
    repository: redis
    tag: 7-alpine
    pullPolicy: IfNotPresent
  
  ports:
    - name: redis
      containerPort: 6379
      protocol: TCP
  
  resources:
    requests:
      memory: 64Mi
      cpu: 50m
    limits:
      memory: 128Mi
      cpu: 100m

# Service configuration for Redis:
services:
  redis:
    enabled: true
    type: ClusterIP
    ports:
      - name: redis
        port: 6379
        targetPort: 6379
        protocol: TCP
