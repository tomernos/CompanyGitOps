# Deployment Manifest - Auto-generated by CI
# Application: {{APP_NAME}}
# Environment: {{ENV_NAME}}
# DO NOT manually edit - CI will overwrite changes
# Last updated: {{TIMESTAMP}}

version: "{{VERSION}}"
ecr_registry: "{{AWS_ACCOUNT_ID}}.dkr.ecr.{{AWS_REGION}}.amazonaws.com/{{APP_NAME}}"
ecr_repo: "{{APP_NAME}}"
backend_image: "{{ECR_REGISTRY}}:backend-{{VERSION}}"
frontend_image: "{{ECR_REGISTRY}}:frontend-{{VERSION}}"
deployed_at: "{{TIMESTAMP}}"
deployed_by: "jenkins"
git_commit: "{{GIT_COMMIT}}"
git_branch: "{{GIT_BRANCH}}"

